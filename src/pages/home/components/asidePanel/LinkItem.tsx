// import { Button, Divider } from "@/shared/ui";
// import { FC } from "react";
// import { NavLink } from "react-router-dom";

// interface LinkItem {
//   link: {
//     text: string;
//     to: string;
//     disabled?: boolean;
//     divider?: boolean;
//   };
// }

// const LinkItem: FC<LinkItem> = ({ link }) => {
//   return (
//     <>
//       <NavLink to={link.to} className={`aside-panel__link ${link.disabled ? "disabled" : ""}`}>
//         <Button label={link.text} fullWidth justifyContent="start" variant="tertiary" />
//       </NavLink>
//       {link.divider && <Divider />}
//     </>
//   );
// };

// export default LinkItem;
